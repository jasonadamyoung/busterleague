<% @page_title = boxscore_title(@boxscore) %>

<div class="page-header" id="overview">
  <h1>
    <%= link_to(@boxscore.home_team.name, team_path(@boxscore.home_team))%>
    vs.
    <%= link_to(@boxscore.away_team.name, team_path(@boxscore.away_team))%>
    - <%= @boxscore.date.strftime("%b %d")%>
  </h1>
</div>

  <div class="page-header">
    <h1>Boxscore</h1>
  </div>

  <div class="row">
    <div class="col-md-12">
      <pre><%= @boxscore.content.join("\n") %></pre>
    </div>
  </div>
