apiVersion: v1
kind: Service
metadata:
  name: busterleague
  labels:
    app: "busterleague"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: "busterleague"